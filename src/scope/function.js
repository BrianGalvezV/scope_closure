function greeting() {
    let userName = 'Ana'
    console.log(userName)

    if (userName === 'Ana'){
        console.log(`hola ${userName}!`)
    }
}

greeting();
//console.log(userName) // la varieble userName solo es accesible dentro de la funcion